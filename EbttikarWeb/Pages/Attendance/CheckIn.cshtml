@page
@using Microsoft.AspNetCore.Identity
@model EmployeeManagement.Pages.Attendance.CreateModel

@{
    ViewData["Title"] = "Check In";
}

<h1>Attendance</h1>

<h4>Checking In</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <input asp-for="EmployeeAttendance.EmployeeId" type="hidden" class="form-control"/>
                <span asp-validation-for="EmployeeAttendance.EmployeeId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EmployeeAttendance.CheckIn" class="control-label"></label>: @DateTime.Now
                <input asp-for="EmployeeAttendance.CheckIn" type="hidden" class="form-control" value="@DateTime.Now" />
                <span asp-validation-for="EmployeeAttendance.CheckIn" class="text-danger"></span>
            </div>
            <p />
            <div class="form-group">
                <button class="btn btn-primary" type="submit">
                    <i class="bi bi-bell"></i>&nbsp; Check In
                </button>
            </div>
        </form>
    </div>
</div>

<hr />
<partial name="_BackToList" />

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
