@page
@model EmployeeManagement.Pages.Attendance.Justifications.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Justifications</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Justification[0].JustificationId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Justification[0].DateCreated)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Justification[0].EmployeeId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Justification[0].Reason)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Justification) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.JustificationId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateCreated)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Reason)
            </td>
            @*<td>
                <a asp-page="./Edit" asp-route-id="@item.JustificationId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.JustificationId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.JustificationId">Delete</a>
            </td>*@
            <td class="w-25">
                <div class="w-auto btn-group">
                    <a asp-page="./Details" asp-route-id="@item.JustificationId" class="btn btn-secondary btn-outline-light">
                        <i class="bi bi-info-circle"></i>&nbsp; Details
                    </a>
                    <a asp-page="./Edit" asp-route-id="@item.JustificationId" class="btn btn-secondary btn-outline-light ">
                        <i class="bi bi-pencil-square"></i>&nbsp; Edit
                    </a>
                </div>
            </td>
        </tr>
}
    </tbody>
</table>
